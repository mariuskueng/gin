{
  "name": "gin",
  "version": "0.5.1",
  "description": "JavaScript Markdown Editor for Mac",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager ./ Gin --out=dist --overwrite --platform=darwin --arch=x64 --version=0.30.6 --app-version=0.5.1 --icon=./public/assets/Gin.icns"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:mariuskueng/gin.git"
  },
  "keywords": [
    "markdown",
    "electron"
  ],
  "author": "Marius Küng",
  "bugs": {
    "url": "https://github.com/mariuskueng/gin/issues"
  },
  "homepage": "https://github.com/mariuskueng/gin",
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {
    "electron-debug": "^0.1.1",
    "showdown": "^1.2.1"
  },
  "devDependencies": {
    "electron-packager": "^5.0.0",
    "electron-prebuilt": "^0.30.6"
  }
}
